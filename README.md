# Color-Sorting-Robot
The system detects ball colors based on primary colors. A sensor identifies the ball’s color from manual input, and if matched, the ball is picked and placed in the corresponding box; otherwise, it is ignored. Six motors enable rotation and translation of the framework, while microcontroller controls the entire process through software.
